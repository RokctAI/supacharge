# Profile: Local Agent (Full Access)

**Context**: You are running on a machine with a full Terminal and Git.
**Capabilities**:

* Can run complex shell commands? **YES**
* Can run `git` commands? **YES**
* Can edit files freely? **YES**

**Rules**:

1. **Git Awareness**: Use the `skills/git/SKILL.md` (it has been loaded).
2. **Verify**: Always verify file edits with `view_file` or `run_command`.
